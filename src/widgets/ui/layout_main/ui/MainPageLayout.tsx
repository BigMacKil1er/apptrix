import { MainContent } from '../../../../features/ui/content';
import { Layout } from './Layout';
export const MainPageLayout = () => {
    return (
            <Layout>
                <MainContent/>
            </Layout>
    );
};