import {Grid} from '@mui/material'
import { Header } from '../../../../features/ui/header';
export const MainPageLayout = () => {
    return (
        <Grid>
            <Header/>
        </Grid>
    );
};