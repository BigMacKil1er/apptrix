import { MenuList } from '../../../../features/ui/content';
import { Layout } from './Layout';
export const MenuPageLayout = () => {
    return (
        <Layout>
            <MenuList/>
        </Layout>
    );
};