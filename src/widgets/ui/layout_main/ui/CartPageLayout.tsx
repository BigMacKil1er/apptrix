import { Cart } from "../../../../features/ui/content";
import { Layout } from "./Layout";

export const CartPageLayout = () => {
    return (
        <Layout >
            <Cart/>
        </Layout>
    );
};